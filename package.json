{
  "name": "openclaw-exec-pty",
  "version": "1.0.0",
  "type": "module",
  "description": "PTY-based shell execution plugin for OpenClaw - runs commands in interactive shells for proper environment initialization",
  "main": "./index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "oxlint src",
    "format": "oxfmt src"
  },
  "keywords": [
    "openclaw",
    "openclaw-plugin",
    "pty",
    "shell",
    "exec"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@sinclair/typebox": "^0.34.0"
  },
  "peerDependencies": {
    "openclaw": ">=2026.1.0",
    "@lydell/node-pty": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
